jQuery(function($){
	/*
	Urb.setupGallery = function() {
		$('.gallery').masonry({
			// options
			itemSelector: '.gallery-item',
			columnWidth: 200,
			fitWidth: true
		});
	};

	Urb.$window.on('ajaxload load', Urb.setupGallery);
	*/
});